# Example: Java Microservice Repository Output

## Input Manifest

```yaml
project:
  name: payment-gateway
  project_type: greenfield
  domain: generic
technology:
  backend:
    language: java
    version: 21
    framework: spring-boot
  database:
    type: aurora-postgresql
    migration_tool: flyway
    orm: spring-data-jpa
  messaging:
    broker: sqs
    pattern: event-driven
architecture:
  style: microservices
  patterns: [ddd, outbox, api-gateway]
cloud:
  provider: aws
  infra_as_code: cdk
governance:
  profile: standard
ai:
  enabled: false
```

## Generated Output Summary

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  EEIK Repository Generator — Complete
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Project:         payment-gateway
Type:            greenfield
Domain:          generic

Generated:
  ✅ Directory structure (34 directories)
  ✅ Source skeleton (18 files)
  ✅ Infrastructure stubs (4 CDK stacks)
  ✅ CI/CD workflows (3 pipeline files)
  ✅ .claude/ configuration (5 agents)
  ✅ .github/ configuration
  ✅ Governance artifacts (3 documents)

Capability Packs Applied:
  ✅ architecture-pack
  ✅ java-pack
  ✅ aws-pack
  ✅ governance-pack (standard)

Patterns Applied:
  ✅ DDD (hexagonal package structure)
  ✅ Outbox (OutboxEvent entity + migration)
  ✅ API Gateway (CDK ApiGatewayStack)

Next Steps:
  1. Review generated CLAUDE.md and customise
  2. Replace TODO placeholders in skeleton files
  3. Set real account IDs in cdk.context.json
  4. Run /validate-manifest to confirm alignment
  5. Push to feature branch and open PR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Key Generated Files

### `src/main/java/com/org/paymentgateway/domain/model/Payment.java`

```java
package com.org.paymentgateway.domain.model;

import java.math.BigDecimal;
import java.time.Instant;
import java.util.UUID;

/**
 * Payment Aggregate Root
 * Generated by: EEIK Repository Generator
 * Pattern: DDD Aggregate
 */
public class Payment {

    private final UUID id;
    private final BigDecimal amount;
    private final String currency;
    private PaymentStatus status;
    private final Instant createdAt;

    private Payment(UUID id, BigDecimal amount, String currency) {
        this.id = id;
        this.amount = amount;
        this.currency = currency;
        this.status = PaymentStatus.PENDING;
        this.createdAt = Instant.now();
    }

    public static Payment initiate(BigDecimal amount, String currency) {
        // TODO: Add domain validation
        return new Payment(UUID.randomUUID(), amount, currency);
    }

    // TODO: Add domain behaviours (confirm, reject, refund)
    // TODO: Add getters
}
```

### `src/main/java/com/org/paymentgateway/infrastructure/persistence/OutboxEvent.java`

```java
package com.org.paymentgateway.infrastructure.persistence;

import jakarta.persistence.*;
import java.time.Instant;
import java.util.UUID;

/**
 * Outbox Pattern — transactional event store
 * Generated by: EEIK Repository Generator
 */
@Entity
@Table(name = "outbox_events")
public class OutboxEvent {

    @Id
    @GeneratedValue
    private UUID id;

    @Column(nullable = false)
    private String eventType;

    @Column(nullable = false, columnDefinition = "jsonb")
    private String payload;

    @Enumerated(EnumType.STRING)
    private OutboxStatus status = OutboxStatus.PENDING;

    private Instant occurredAt = Instant.now();
    private Instant processedAt;

    // TODO: Add constructor, getters, and markProcessed()
}
```

### `infrastructure/cdk/lib/application-stack.ts`

```typescript
import * as cdk from 'aws-cdk-lib';
import * as ecs from 'aws-cdk-lib/aws-ecs';
import * as ec2 from 'aws-cdk-lib/aws-ec2';
import { Construct } from 'constructs';

/**
 * Application Stack — ECS Fargate Service
 * Generated by: EEIK Repository Generator
 * Service: payment-gateway
 */
export class ApplicationStack extends cdk.Stack {

  constructor(scope: Construct, id: string, props: ApplicationStackProps) {
    super(scope, id, props);

    const cluster = new ecs.Cluster(this, 'PaymentGatewayCluster', {
      vpc: props.vpc,
      containerInsights: true,
    });

    const taskDefinition = new ecs.FargateTaskDefinition(this, 'TaskDef', {
      memoryLimitMiB: 1024,
      cpu: 512,
    });

    taskDefinition.addContainer('PaymentGateway', {
      // TODO: Replace with actual ECR image URI
      image: ecs.ContainerImage.fromRegistry('amazon/amazon-ecs-sample'),
      logging: ecs.LogDriver.awsLogs({ streamPrefix: 'payment-gateway' }),
      environment: {
        SPRING_PROFILES_ACTIVE: props.environment,
      },
      portMappings: [{ containerPort: 8080 }],
    });

    // TODO: Add ALB, auto-scaling, security groups
    cdk.Tags.of(this).add('Service', 'payment-gateway');
    cdk.Tags.of(this).add('Environment', props.environment);
  }
}

interface ApplicationStackProps extends cdk.StackProps {
  vpc: ec2.IVpc;
  environment: string;
}
```
