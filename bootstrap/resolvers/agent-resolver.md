# Agent Resolver

Purpose:

Determine which agents should exist in the generated repository.

Rules:

Use existing reusable agents first.

Generate new agents only when:

- No suitable agent exists
- Project requires specialization
- Domain requires unique expertise

Output:

agent-selection.yaml
